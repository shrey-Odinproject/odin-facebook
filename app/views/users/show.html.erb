<h1><%= @user.name%></h1>
<p><%= @user.email%></p>

<% if current_user != @user %>
    <%= button_to "Send Friend Request", create_request_user_path %>
<% else %> 
    show all the incoming requests so u can accept them
<% end %>
